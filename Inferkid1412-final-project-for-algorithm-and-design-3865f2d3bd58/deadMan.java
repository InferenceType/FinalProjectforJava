import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class deadMan here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class deadMan extends Actor
{
    /**
     * 
     */
    public deadMan()
    {
        GreenfootImage image = getImage();  
        image.scale(20,105);
        setImage(image);
    }
    /**
     * Act - do whatever the deadMan wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {
        // Add your action code here.
    }    
}
